# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# PyInstaller / packaged binary output (built by CI)
build/
dist/
*.spec
# except our curated Windows two-binary build spec — CI builds from it
!mcp-keep.spec
*.exe

# Config (may contain a bearer token) — runtime config lives in ~/.mcp-keep
config.json

# Downloaded integration packs (runtime packs live in ~/.mcp-keep/integrations)
integrations/

# Claude Code local working files (CLAUDE.md is intentionally shipped as the setup guide)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
